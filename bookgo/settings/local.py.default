DEBUG = True

ALLOWED_HOSTS = ['*']

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'bookcrossing',
        'USER': 'bookcrossing',
        'PASSWORD': 'bookcrossing',
    }
}

INTERNAL_IPS = [
    '127.0.0.1'
]

ADMINS = [('Dima', 'dmyutro@ukr.net')]

EMAIL_USE_TLS = True
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_HOST_USER = 'email@gmail.com'  # Change it
EMAIL_HOST_PASSWORD = 'password'  # Change it
EMAIL_PORT = 587
